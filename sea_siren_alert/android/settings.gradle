pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        // Add Flutter's build plugin repo
        includeBuild("../flutter")
    }
}

plugins {
    id "dev.flutter.flutter-gradle-plugin"
}

include(":app")
